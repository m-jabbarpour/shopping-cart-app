import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#203040",
    },
    secondary: {
      main: "#f0c041",
    },
  },
  typography: {
    fontFamily: "Segoe UI",
    button: {
      fontFamily: "Arial",
    },
  },
});
